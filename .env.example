# ===========================================
# Buwate Tennis Club - Environment Variables
# ===========================================
# Copy this file to .env and fill in your values.
# Never commit .env to version control.

# ----- Server -----
NODE_ENV=development
PORT=5000

# Required: PostgreSQL connection string (Supabase or any Postgres)
# Example: postgresql://user:password@host:5432/database?sslmode=require
DATABASE_URL=

# Required in production: strong random secret for session signing
# Generate with: openssl rand -base64 32
SESSION_SECRET=

# Optional: AI chat (Lovable gateway). Omit to disable /api/chat.
LOVABLE_API_KEY=

# ----- Client (Vite - prefix with VITE_) -----
# Required for Supabase auth and role dashboards
# From Supabase: Project Settings > API
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
